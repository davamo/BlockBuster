    <%= link_to 'New rental', new_rental_path, class: 'btn btn-success' %>
<table>
  <thead>
    <tr>
      <th>Cliente</th>
      <th>Películas arrendadas</th>
    </tr>
  </thead>
  <tbody>
   <% @rentals.each do |rental| %>
      <tr>
        <td><%= rental.customer.name %></td>
        <td><%= rental.customer.movies.count %></td>
      </tr>
    <% end %>
  </tbody>
</table>

