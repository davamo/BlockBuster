<div class="text-end">  <%= link_to '+ Rent Movie', new_rental_path, class: 'btn btn-success' %> </div>
<table class="table">
  <thead>
    <tr>
      <th>Cliente</th>
      <th>Películas arrendadas</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
   <% @rentals.each do |rental| %>
      <tr>
        <td><%= rental.customer.name %></td>
        <td><%= rental.customer.movies.count %></td>
        <td>
          <div class="btn-group" role="group">
              <%= link_to 'Edit', edit_rental_path(rental), class: 'btn btn-primary' %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

